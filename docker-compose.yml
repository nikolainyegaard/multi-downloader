services:
  multi-downloader:
    image: ghcr.io/nikolainyegaard/multi-downloader:dev
    container_name: multi-downloader
    restart: unless-stopped
    environment:
      TZ: "Europe/Oslo"
    ports:
      - "127.0.0.1:8000:8000"
